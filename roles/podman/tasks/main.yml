---
- name: Create /mnt/data/var/tmp
  ansible.builtin.file:
    path: /mnt/data/var/tmp
    state: directory
    mode: '0777'

- name: Install podman
  become: true
  ansible.builtin.apt:
    pkg:
      - podman
    state: present
    update_cache: true
