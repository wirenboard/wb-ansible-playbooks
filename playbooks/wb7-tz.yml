---
  - name: WB7 timezone playbook
    gather_facts: false
    hosts: all
    remote_user: root
    vars_files:
      - ../config.yml
    tasks:
      - name: Set timezone
        community.general.timezone:
          name: '{{ tz }}'
          hwclock: local
